<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dituhui.saas.mapper.TestMapper">
	 <select id="getAll">
       SELECT *
       FROM users
    </select>

    <select id="getOne">
        SELECT *
       FROM users
       WHERE id = #{id}
    </select>

    <insert id="insert">
       INSERT INTO 
            users
            (userName,passWord,user_sex) 
        VALUES
            (#{userName}, #{passWord}, #{userSex})
    </insert>

    <update id="update">
       UPDATE 
            users 
       SET 
        <if test="userName != null">userName = #{userName},</if>
        <if test="passWord != null">passWord = #{passWord},</if>
        nick_name = #{nickName}
       WHERE 
            id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.String" >
       DELETE FROM
             net_point_info 
       WHERE 
             pointName =#{pointName}
    </delete>
</mapper>